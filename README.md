# 五子棋游戏

一款使用 HTML5 Canvas 和 JavaScript 实现的经典策略棋盘游戏。两名玩家轮流在 15×15 的棋盘上放置棋子，目标是首先在水平、垂直或对角线上连成五个棋子。

## 功能特点

- **精美界面**: 现代设计，渐变背景和平滑动画
- **交互式画布**: 点击放置棋子，提供视觉反馈
- **游戏逻辑**: 完整的规则实现，包括胜利检测
- **响应式设计**: 适用于桌面和移动设备
- **视觉效果**:
  - 棋子放置动画
  - 获胜棋子高亮显示
  - 下一步棋的悬停预览
- **游戏控制**:
  - 重新开始游戏
  - 撤销上一步
  - 显示提示（简单AI建议）

## 游戏玩法

1. **目标**: 将你的五个棋子连成一行（水平、垂直或对角线）
2. **回合**: 黑棋先走，然后玩家轮流进行
3. **放置**: 点击任意空交叉点放置你的棋子
4. **获胜**: 游戏自动检测玩家何时获胜
5. **重新开始**: 使用“重新开始游戏”按钮开始新游戏

## 项目结构

```
├── index.html          # 主HTML文件
├── css/
│   └── style.css      # 游戏所有样式
├── js/
│   ├── utils.js       # 工具函数（坐标转换、验证）
│   ├── board.js       # 游戏逻辑和状态管理
│   └── game.js        # 主游戏控制器和Canvas渲染
├── server.js          # 本地测试的简单HTTP服务器
└── README.md          # 本文件
```

## 安装和运行

### 快速开始

1. **克隆或下载** 项目文件
2. **启动本地服务器**:
   ```bash
   node server.js
   ```
3. **打开浏览器** 并访问:
   ```
   http://localhost:8000/
   ```

### 替代方法

**使用 Python**（如果 Node.js 不可用）:

```bash
python -m http.server 8000
```

**直接打开文件**（由于模块的CORS限制，不推荐）:
直接在浏览器中打开 `index.html`（可能无法与ES6模块一起工作）。

## 技术细节

### 游戏架构

- **棋盘类** (`board.js`): 管理游戏状态、移动验证、胜利检测
- **游戏控制器** (`game.js`): 处理Canvas渲染、用户交互、动画
- **工具函数** (`utils.js`): 坐标转换、验证辅助函数

### 关键算法

**胜利检测**:

- 从最后放置的棋子检查8个方向
- 计算相同颜色的连续棋子数量
- 当计数 ≥ 5 时返回胜利

**坐标系统**:

- 屏幕坐标 ↔ 棋盘坐标转换
- 处理响应式画布大小调整

### 依赖项

- **无**: 纯 HTML5、CSS3 和 JavaScript (ES6+)
- **Font Awesome**: UI元素的图标（从CDN加载）
- **无框架**: 轻量级且快速

## 浏览器兼容性

- Chrome 61+（支持ES6模块）
- Firefox 60+
- Safari 10.1+
- Edge 16+

注意：由于使用ES6模块，不支持Internet Explorer。

## 开发

### 从源码构建

此项目使用原生ES6模块，因此无需构建步骤。所有文件都可以直接运行。

### 代码风格

- 使用模块导入/导出的ES6+ JavaScript
- 游戏组件使用类
- 描述性的变量和函数名
- 复杂逻辑有注释

### 测试

手动测试包括:

1. 基本游戏流程
2. 获胜条件（水平、垂直、对角线）
3. 边界情况（棋盘边界、无效移动）
4. UI响应性

## 游戏截图

_（实际项目中会添加截图）_

1. **游戏开始**: 黑棋在空棋盘上的回合
2. **游戏中局**: 放置了几个棋子，可见悬停预览
3. **获胜状态**: 五个黑棋连成一线，金色高亮显示

## 未来增强

未来开发的潜在功能:

1. **AI对手**: 实现带有alpha-beta剪枝的Minimax算法
2. **游戏模式**: 限时游戏、不同棋盘尺寸
3. **在线多人游戏**: 基于WebSocket的实时游戏
4. **游戏记录**: 保存和回放之前的游戏
5. **难度级别**: 可调节的AI强度

## 致谢

- **游戏设计**: 传统五子棋规则
- **实现**: 作为HTML5 Canvas和JavaScript的演示创建
- **图标**: Font Awesome（免费版本）
- **配色方案**: 自定义渐变设计

## 许可证

此项目可用于教育和个人用途。可以自由修改和分发，但需注明出处。

## 贡献

1. Fork 代码库
2. 创建功能分支
3. 进行更改
4. 彻底测试
5. 提交拉取请求

## 支持

如果遇到任何问题:

1. 检查浏览器控制台是否有错误（F12）
2. 确保使用现代浏览器
3. 验证服务器是否正确运行
4. 检查所有文件是否在正确位置

---

享受游戏！与你的朋友来一场策略和远见的较量。
